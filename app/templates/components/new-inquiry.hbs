{{#if addNewInquiry}}
  <h1>New Question</h1>
  <div>
    <form>
      <div class="form-group">
        <label for="author">Author</label>
        {{input value=author id="author"}}
      </div>

      <div class="form-group">
        <label for="greatQuestion">Question</label>
        {{textarea value=greatQuestion id="greatQuestion"}}
      </div>

      <div class="form-group">
        <label for="notes">Write some notes here</label>
        {{textarea value=notes id="notes"}}
      </div>
      <button class ="btn-info" {{action 'saveInquiry'}}>Save</button>
    </form>
  </div>
{{else}}
  <button class ="btn-info"{{action 'inquiryFormShow'}}>New Question</button>
{{/if}}
